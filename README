kill all .DS_Store

1. clone and install

  $ git clone https://github.com/slmjkdbtl/dskill
  $ cd dskill
  $ make install

2. kill existing .DS_Store first

  $ dskill kill ~

3. start service to prevent creation of .DS_Store

  $ dskill service start

4. enjoy life
